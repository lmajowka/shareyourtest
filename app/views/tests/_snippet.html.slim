= link_to exam do 
  div class='mr40 mt20 test-snippet-container' 
    div class='fl test-snippet-image'
      = image_tag(exam.picture.url(:thumb))
    div class='fl test-snippet-info'  
      h3 class='fs20'
        = exam.title
      div
        = truncate(exam.description, length:90)
      div class='fs12'
        = exam.questions.size
        = ' Questions' 
